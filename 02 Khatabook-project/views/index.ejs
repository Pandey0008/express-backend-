<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/public/css/style.css">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 text-white font-['Helvetica']">
    <main class="w-full h-screen flex flex-col">
        
        <nav class="flex px-5 sm:px-10 justify-between py-5 bg-zinc-800">
            <h3 class="text-2xl tracking-tight text-white">Khaatabook</h3>
            <div class="navlinks flex gap-5">
                <a class="tracking-tight text-white" href="/">Home</a>
                <a class="tracking-tight text-white" href="/create">Create New Hisaab</a>
            </div>
        </nav>

       
        <div class="px-5 sm:px-10 hisaabs flex-grow">
            <h3 class="capitalize text-2xl font-medium mb-5 mt-10 tracking-tight text-white">All hisaab kitaab</h3>

            <% if (files.length > 0) { %>
                <% files.forEach((file) => { %>
                    <div class="hisaab w-full sm:w-fit items-center py-3 mb-3 px-5 gap-20 bg-blue-500 flex justify-between rounded-md">
                        <div class="flex gap-4 text-white items-center">
                            <h3 class="text-xl sm:text-2xl">
                                <%= file.split('.txt')[0] %>
                            </h3>
                            <a href="/hisaab/<%= file %>">
                                <i class="w-6 h-6 text-blue-500 flex items-center justify-center bg-white rounded-full ri-arrow-right-line" aria-label="View Hisaab"></i>
                            </a>
                        </div>
                        <div class="flex gap-3 items-center">
                            <a href="/edit/<%= file %>">
                                <i class="text-white ri-pencil-line" aria-label="Edit Hisaab"></i>
                            </a>
                            <a href="/delete/<%= file %>">
                                <i class="w-6 h-6 text-red-500 flex items-center justify-center bg-white rounded-full ri-delete-bin-line" aria-label="Delete Hisaab"></i>
                            </a>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <h3 class="text-zinc-500 text-white">Currently nothing to show, create hisaabs to see something.</h3>
            <% } %>
        </div>
    </main>
</body>

</html>
